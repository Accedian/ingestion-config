# Ribbon

- source protocol: gNMI
- current scope: partial Telegraf collector scaffolding and local sample artifacts only

Important note:

- this integration work is incomplete and requires additional work before it can be treated as a validated ingestion artifact
- the source did not produce a usable end-to-end golden bundle in the capture pass used for this branch
- preserve these files as a starting point, not as a finished reference implementation
